{
  "name": "summer-framework",
  "version": "0.0.6",
  "main": "index.js",
  "license": "MIT",
  "type": "index.d.ts",
  "dependencies": {
    "@types/express": "^4.17.2",
    "express": "^4.17.1",
    "typescript": "^3.7.5"
  },
  "scripts": {
    "prepublish": "yarn build",
    "build": "tsc",
    "test": "yarn build && npx ts-node ./__tests__/index.spec.ts",
    "lint": "npx prettier --write '{**,.}/*.ts'"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.5.4",
    "node-fetch": "^2.6.0"
  }
}
